missions:
  check-rust:
    image: rust
    script: cargo check
  tests:
    image: rust
    recipe: |
      FROM rust
      apt-get update
      apt-get install -y docker.io
    script: |
      cargo test
  clippy:
    image: rust
    script: |
      rustup component add clippy
      cargo clippy
shell:
  image: rust
